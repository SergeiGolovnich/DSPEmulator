<UserControl x:Class="DSPEmulatorUI.Views.SignalDefinitionEffectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DSPEmulatorUI.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" FontWeight="Bold">Signal Definition</TextBlock>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Slider x:Name="DefinitionBits" Maximum="16" Minimum="1" Orientation="Horizontal" Width="150" HorizontalAlignment="Center" SmallChange="1" TickPlacement="Both" IsDirectionReversed="True"/>
            <TextBlock Text="{Binding ElementName=DefinitionBits, Path=Value, StringFormat={}{0} definition_bits}" HorizontalAlignment="Center"/>
        </StackPanel>

    </Grid>
</UserControl>
